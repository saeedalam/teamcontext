# Binaries
teamcontext
teamcontext.exe
dist/
build/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test
coverage.out
coverage.html

# TeamContext runtime (these are per-project, not part of the tool source)
# Note: .teamcontext/ directories in USER projects are git-tracked by design.
# The SQLite cache inside .teamcontext is gitignored per-project via .teamcontext/.gitignore
